// load the base configuration
exec default.cfg

// load the last saved configuration
exec config.cfg

// run the developer configuration if present
exec developer.cfg

// run a user script file if present
exec autoexec.cfg
